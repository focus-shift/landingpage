<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    {% set title = 'Focus Shift' %} {% if meta.title %} {% set title = title + '
    ' + meta.title %} {% endif %}
    <title>{{ title | escape }}</title>
    <meta name="og:title" content="{{ title | escape }}" />
    {% if meta.description %}
    <meta name="description" content="{{ meta.description | escape }}" />
    <meta property="og:description" content="{{ meta.description | escape }}" />
    {% endif %}

    <link
      rel="preload"
      href="/fonts/roboto-v20-latin-regular.woff2"
      as="font"
      type="font/woff2"
      crossorigin="anonymous"
    />
    <link
      rel="preload"
      href="/fonts/roboto-v20-latin-700.woff2"
      as="font"
      type="font/woff2"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <script src="/js/hello.js"></script>
  </head>
  <body class="flex flex-col items-stretch">
    <header
      class="w-full max-w-screen-lg flex flex-col mx-auto mb-12 xl:mb-16 pb-12 px-4 xl:px-0 border-gray-300 border-b"
    >
      {% include "partials/header.html" %}
    </header>

    <main class="flex-1 mb-12 px-4 xl:px-0">
      <div class="mx-auto max-w-screen-lg">
        {% block content %}{{ content | safe }}{% endblock %}
      </div>
    </main>
  </body>
</html>
