<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    {% set title = 'Focus Shift - Freiraum für dein Kerngeschäft' %}
    {% if meta.title %}
    {% set title = title + ' ' + meta.title %}
    {% endif %}
    <title>{{ title | escape }}</title>
    <meta name="og:title" content="{{ title | escape }}" />
    {% set description = 'SaaS-Produkte zur Digitalisierung deiner Unternehmensprozesse - Remote, Mobile, Online! Starte jetzt mit der Urlaubsverwaltung - Urlaubsverwaltung.cloud' %}
    {% if meta.description %}
        {% set description = description + ' ' + meta.description %}
    {% endif %}

    <meta name="description" content="{{ description | escape }}">
    <meta property="og:description" content="{{ description | escape }}">

    <link
      rel="preload"
      href="/fonts/roboto-v20-latin-regular.woff2"
      as="font"
      type="font/woff2"
      crossorigin="anonymous"
    />
    <link
      rel="preload"
      href="/fonts/roboto-v20-latin-700.woff2"
      as="font"
      type="font/woff2"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <script src="/js/hello.js"></script>
  </head>
  <body class="flex flex-col">
    <div class="flex flex-col min-h-screen">
      <header
        class="w-full max-w-screen-lg flex flex-col mx-auto mb-12 xl:mb-16 pb-12 py-8 px-4 xl:px-0 border-gray-300 border-b"
      >
        {% include "partials/header.html" %}
      </header>
      <main class="flex-1 mb-12 px-4 xl:px-0">
        <div class="mx-auto max-w-screen-lg">
          {% block content %}{{ content | safe }}{% endblock %}
        </div>
      </main>
    </div>
    <footer class="bg-black text-white font-light">
      <div class="max-w-screen-lg mx-auto py-8 px-4 xl:px-0">
        {% include "partials/footer.html" %}
      </div>
    </footer>
  </body>
</html>
